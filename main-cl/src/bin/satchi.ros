#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.satchi.3848266991
  (:use :cl))
(in-package :ros.script.satchi.3848266991)
(ql:quickload :satchi-bin-satchi)

(defun main (&rest argv)
  (declare (ignorable argv))
  (satchi.bin.satchi:main))
;;; vim: set ft=lisp lisp:
